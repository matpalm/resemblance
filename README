see http://matpalm.com/resemblance

run with 
> cat test.data | ruby shingle.rb N result.yaml

0 <= N <= 1

will output all resemblances with a resememblance > N to result.yaml

see the results with
> ./ycat.rb result.yaml

run arbitrary reparsing with
> ./reparse.rb result.yaml

random notes on using the c++ version

> time (head -n 100 full_dataset | cpp/bin/Release/resemblance 0 > compare.100)
> cat compare.100 | ./histo.pl | sort -n > histo.100
> plot.sh histo.100 histo.100.png
